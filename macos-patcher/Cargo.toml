[package]
name = "ltk-macos-patcher"
version.workspace = true
edition = "2021"
license = "MIT"
publish = false

[dependencies]
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
cc = "1"

[dev-dependencies]
tempfile = "3"
